// Template for secrets.h — copy this file to secrets.h (gitignored)
// and fill in real values before building. See ../../docs/wiring.md
// for hardware bring-up and README.md for the build steps.
#ifndef SECRETS_H
#define SECRETS_H

#define SSID_STR        "YOUR_WIFI_SSID"
#define WIFI_PW_STR     "YOUR_WIFI_PASSWORD"

#define MQTT_USERNAME    ""   // leave empty if the broker allows anonymous connections
#define MQTT_PASSWORD    ""

#endif
